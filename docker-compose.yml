version: '3.6'
services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    hostname: postgres
    container_name: postgres
    ports:
      - 5432:5432
